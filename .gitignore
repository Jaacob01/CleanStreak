# 环境配置(含密钥,不入库;.env.example 保留)
# 注意:只忽略含密钥的两份;frontend_reactnative/.env 不含密钥,且 EAS 打包依赖它(EXPO_PUBLIC_API_URL)
/.env
/fastapi_server/.env

# Python 虚拟环境与编译产物
venv/
__pycache__/
*.pyc

# macOS
.DS_Store
